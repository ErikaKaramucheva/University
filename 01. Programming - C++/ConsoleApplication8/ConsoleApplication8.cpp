#include <iostream>
using namespace std;
#include<string>
#include<cmath>

int main()
{
    /*int num[7] = { 5,12,17,11,3,42,18 };
    int n = sizeof(num)/ sizeof(num[7]);
    int max = 0;
    for (int i = 0; i < n; i++)
    {
        cout << num[i]<<endl;  
        if (num[i] > max) {
            max = num[i];
        } 
    }cout << max << " is the largest element of a given array of integers.";*/
    
//2. - Write a C++ program to find the largest three elements in an array.
   /* int arr[7] = {2,5,7,13,15,154,4645};
    int n = sizeof(arr) / sizeof(arr[1]);
    int num1 = 0;
    int num2 = 0;
    int num3 = 0;
    for (int i = 0; i < n; i++)
    {
        cout << arr[i] << endl;
        if (arr[i] > num1) {
            num3 = num2;
            num2 = num1;
            num1 = arr[i];
        }
        else if (arr[i] > num2) {
            num3 = num2;
            num2 = arr[i];
        }
        else if (arr[i] > num3) {
            num3 = arr[i];
        }
    } cout << num1 << endl << num2 << endl << num3;*/

    
//3. - Write a C++ program to find the smallest element in a given array of integers.
    /*int num[5] = {7,12,11,3,1};
    int n = sizeof(num) / sizeof(num[5]);
    int min = 1000;
    for (int i = 0; i < n; i++)
    {
        cout << num[i] << endl;
        if (num[i] < min)
        {
            min = num[i];
        }
    } cout << min << " is the smallest element in a given array of integers." << endl;*/
}
